```bash
#실행 방법
git clone https://github.com/SuaAppa/opggclone.git
으로 소스를 복사합니다.
opggclone 폴더가 생성되는데 해당 폴더의 터미널에서
npm install
이후
npm run dev 로 실행할 수 있습니다.
실행 후, http://localhost:3000 으로 접속해서 확인가능합니다.
```

```bash
#기타 사항
1. Next.js --typescript , tailwindCSS 를 사용했습니다.
2. 노션에는
KDA : 25.9 / 15.8 / 14.1 == kills / assists / deaths 로 안내하셨지만
kills / deaths / assists 가 맞는듯 하여 순서 변경하였습니다.
3. zeplin, swagger 에 제공되지 않은 부분은 임의로 하였습니다.
```

```bash
#후기
먼저 기회를 주셔서 감사합니다.
평일 장거리 출퇴근으로 인해 과제 수행 시간을 내기 쉽지 않았는데
해당 부분 고려해주셔서 감사합니다.
```
